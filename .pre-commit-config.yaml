repos:
  - repo: local
    hooks:
      - id: make-format
        name: make format
        entry: bash -c "make format && git add -u"
        language: system
        stages: [commit]
        pass_filenames: false
      - id: make-lint-test-unit
        name: make lint && make test-unit
        entry: bash -c "make lint && make test-unit"
        language: system
        stages: [push]
        always_run: true
        pass_filenames: false
